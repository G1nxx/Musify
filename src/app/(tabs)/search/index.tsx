import { defaultStyles } from '@/styles'
import { View, Text } from 'react-native'

const SearchScreen = () => {
	return (
		<View style={defaultStyles.container}>
			<Text style={defaultStyles.text}>Search</Text>
		</View>
	)
}

export default SearchScreen
